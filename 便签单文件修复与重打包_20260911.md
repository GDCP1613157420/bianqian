# 桌面便签 v6 — 单文件打包缺陷修复 + 三问题修复 + 云端重构建

日期：2026-09-11（GMT+8）

## 核心根因（关键发现）

之前所有通过 `make-single-file.cjs` 生成的 `StickyNotes.html`（含 v5 交付的手机版/免安装版）**实际上是坏的**，会导致白屏或异常。

根因：`make-single-file.cjs` 把内联 JS（`safeJs`）当作**字符串**传给 `String.prototype.replace` 的替换参数。bundle 中含 `$&` / `$\`` / `$'` 等字符，被 JS 的 replace 当成"替换模式"解释，于是把原始 `<script src="./assets/index-FAPK5qKK.js"></script>` 标签（含 `</script>`）又注入回内联脚本里，生成了 4 个多余的外链 `<script>` 标签。HTML 解析器在遇到第一个内联 `</script>`（字节 190768）时**提前闭合**了内联脚本，导致 `createRoot().render()` 永远不执行 → 空白页。这正是用户反复反馈"文件失效/打不开"的真凶（此前误判为腾讯云链接过期）。

修复：改用**函数式 replacer**（`() => '<script>\n'+safeJs+'\n</script>'`），`$` 不再被解释。

修复后校验（StickyNotes.html，224KB）：
- 真实 `</script>` 数量 = 1（仅包裹标签，位于文件末尾）
- 外链 `<script src=` = 0，外链 `<link href=` = 0
- 内联脚本在字节 387 打开、201022 关闭，`createRoot` 位于其内
- jsdom 冒烟测试通过：#root 有子节点、顶栏 📋 与"新建便签"渲染、0 报错

## 本轮另外修复（用户 3 个问题）

1. 顶部菜单被头部挡住 → 改"浮层覆盖"为 master-detail 二选一布局，详情面板内嵌，不再覆盖顶栏（App.tsx）。
2. 本地文件无法打开文件夹 → `tauri.ts` 的 `pickLocalPath` 取真实路径 + 错误反馈；`openExternal` 失败时 `alert` 详细错误。
3. 编辑页字体太小 → DetailPanel.tsx 放大 8 处字号。

（dialog/opener 插件、Capacitor v6、build.yml 安卓 job 已于 run #213 落库，本轮未改。）

## 交付产物（GitHub Actions run #34565741530，success）

- build-windows job → standalone(zip) id 10186043716、installer(zip) id 10186043044
- build-android job → android(zip) id 10185996396
- 下载解压：
  - desktop-sticky-notes.exe（3.39MB，Windows 免安装版）
  - 桌面便签_1.0.0_x64-setup.exe（1.2MB，Windows 安装包）
  - app-debug.apk（4.19MB，安卓）

## 推送记录

经 GitHub Contents API 逐文件 PUT（github.com 直连屏蔽，api.github.com 可用）推送 6 文件：
src/App.tsx、src/components/DetailPanel.tsx、src/tauri.ts、vite.config.web.ts、make-single-file.cjs、StickyNotes.html。

## 交付方式说明

腾讯云 `jsonproxy.3g.qq.com/urlmapper/XXX` 直链现状返回的是**二维码落地页**（非直接下载，桌面端无法直接取文件，且旧链接会过期 405）。因此本轮改用**对话内 MEDIA 附件**直接下发 4 个文件，最可靠。

腾讯云链接（备用，需扫码/可能过期）：
- 单文件 HTML：https://jsonproxy.3g.qq.com/urlmapper/hHQAnp
- Windows 免安装 exe：https://jsonproxy.3g.qq.com/urlmapper/JH2sIO
- Windows 安装包：https://jsonproxy.3g.qq.com/urlmapper/XU2vk5
- 安卓 APK：https://jsonproxy.3g.qq.com/urlmapper/xroA2n

## 待办

- 提醒用户吊销/收窄 GitHub PAT（当前权限过宽，仅 gist 即可）至仅 gist。
- 手机-电脑同步：用户确认手机端形态后再动工（已推荐方案 B：同 Token 读 Gist）。
- 需求 3/4 未决（时间段字段、文件跳转选型）。

## 关键文件

- /Users/a1-6/desktop-sticky-notes/make-single-file.cjs（已修：$ 注入 bug）
- /Users/a1-6/desktop-sticky-notes/StickyNotes.html（已重新生成，已验证可渲染）
- /Users/a1-6/desktop-sticky-notes/src/App.tsx、src/components/DetailPanel.tsx、src/tauri.ts（三问题修复）
- 产物：/tmp/artifacts/{standalone,installer,android}/
